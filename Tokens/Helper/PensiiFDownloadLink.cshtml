@inherits PlantAnApp.Dnn.Tokens.Razor.RazorTemplate
@{
    if (TknParams.FileId != null)
    {
        var fileId = TknParams.FileId.Value.ToString();

        Write("[File:GetFileLinkClick(FileIdentifier=" + fileId + ")]");
    }
}